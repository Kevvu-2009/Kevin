# ---- Core analytics (required, light) ----
numpy>=1.26
pandas>=2.1
scipy>=1.11

# ---- Data / connectivity ----
ccxt>=4.2                # exchange connectivity (OHLCV download, optional live)
SQLAlchemy>=2.0          # ORM for PostgreSQL storage
psycopg2-binary>=2.9     # PostgreSQL driver
redis>=5.0               # state cache / pub-sub
python-dotenv>=1.0       # .env loading

# ---- Backtesting / optimization ----
vectorbt>=0.26           # vectorized backtesting (optional adapter)
Backtesting>=0.3.3       # event-driven backtesting (optional adapter)
optuna>=3.5              # Bayesian optimization

# ---- API / dashboard ----
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
plotly>=5.19
pydantic>=2.6
pydantic-settings>=2.2

# ---- Integrations ----
httpx>=0.27              # async HTTP client (Bullpen REST, misc)
websockets>=12.0         # streaming market data
hyperliquid-python-sdk>=0.9  # Hyperliquid spot/perps trading + public candles

# ---- Utilities ----
structlog>=24.1          # structured logging
tenacity>=8.2            # retry logic
